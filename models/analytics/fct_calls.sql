select
    call_id,
    event_number,
    complaint_number,
    tencode,
    tencode_code,
    disposition_code,
    disposition_description,
    disposition_category,
    shift,
    sector,
    zone,
    block,
    street_name,
    unit_dispatched,
    rpa,
    latitude,
    longitude,
    x_coord,
    y_coord,
    has_geolocation,
    has_disposition,
    call_received_at,
    call_year,
    call_month,
    call_day,
    call_hour,
    call_day_of_week,
    weekend_flag,
    time_period,
    rush_hour_flag

from {{ ref('transformed_calls') }}